# Replace the values below with your actual data in your local secrets.yaml file.
# (DO NOT commit your real secrets.yaml to GitHub!)

wifi_ssid_main: ""
wifi_password_main: ""

wifi_ssid: ""
wifi_password: ""

# Fallback hotspot password if the ESP fails to connect to the Wi-Fi
ap_fallback_password: "sceneswitchesp"

ota_password: ""

# Generate a 32-byte base64 string for the api_encryption_key below using one of these terminal commands:
# - Python     : python -c "import secrets; import base64; print(base64.b64encode(secrets.token_bytes(32)).decode())"
# - PowerShell : $bytes = New-Object Byte[] 32; [Security.Cryptography.RNGCryptoServiceProvider]::Create().GetBytes($bytes); [Convert]::ToBase64String($bytes)
api_encryption_key: ""